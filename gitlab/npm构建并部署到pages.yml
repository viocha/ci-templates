image: node:lts
create-pages:
  pages:
    publish: dist
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
    
  cache:
    paths:
      - node_modules/

  script:
    - npm install
    - npm run build